using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using UnityEngine.UI;

public class SaveScore : MonoBehaviour
{
    //private GameManager gameManager;


    //private int saveScore;
    //[SerializeField] private TMP_Text scoreText;

    //// Start is called before the first frame update
    //void Start()
    //{
    //    saveScore = PlayerPrefs.GetInt("score");
    //    scoreText.text = saveScore.ToString();
    //    gameManager = FindObjectOfType<GameManager>();

    //    gameManager.OnCorrect += SetScore;
    //    gameManager.OnTime += SetScore1;
    //}

    //// Update is called once per frame
    //private void SetScore()
    //{
    //    saveScore = saveScore + 10;
    //    scoreText.text = saveScore.ToString();
    //    PlayerPrefs.SetInt("score", saveScore);

    //}

    //private void SetScore1()
    //{
    //    saveScore = saveScore + 100;
    //    scoreText.text = saveScore.ToString();
    //    PlayerPrefs.SetInt("score", saveScore);

    //}
}
